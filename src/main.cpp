#include "slcanInterface.hpp"

int main() {
    slcanInterface manager;
    // TODO Envoi messages CAN en les tapant puis entrée
    // TODO Affichage messages CAN reçus toutes les secondes
    while (true);
    return 0;
};
